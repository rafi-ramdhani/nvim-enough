-- Any file in lua/user/plugins/ is picked up by lazy.nvim automatically, so
-- you can add plugins without touching anything nvim-enough ships.
--
-- Delete this file, or replace it with something you actually want.

return {
  -- {
  --   "folke/todo-comments.nvim",
  --   event = "VeryLazy",
  --   dependencies = { "nvim-lua/plenary.nvim" },
  --   opts = {},
  -- },
}
